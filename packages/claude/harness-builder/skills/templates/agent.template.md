---
name: {하네스-이름}-{역할-이름}
description: {이 에이전트가 언제 쓰이는가 — 부모가 위임 판단에 쓰는 한 줄}
tools: {필요 툴 목록(쉼표) 또는 줄 삭제 시 전체 상속 — 읽기 전용 역할이면 Read, Grep, Glob 정도로 좁힌다}
model: {선택 — sonnet/opus/haiku 등. 생략 시 부모 모델 상속}
---

<!--
  harness-builder가 하네스의 한 역할(role)을 물질화한 서브에이전트 정의다.
  위치: .claude/agents/{하네스-이름}/{역할-이름}.md
  name은 반드시 {하네스-이름}-{역할-이름}으로 네임스페이스한다 —
  에이전트는 파일 경로가 아니라 name으로 전역 해석되므로, 하네스 간 역할 이름 충돌을 막는다.
  본문은 산출 하네스의 [계약] 역할 분해 행 + 아티팩트 계약에서 파생한다.
  Workflow(ⓒ) 실행 시에는 agent() 호출의 agentType으로 이 name을 지정한다.
  생성 시 이 주석 블록은 삭제한다.
-->

# {역할 이름} 에이전트

> 소속 하네스: {하네스-이름} / 출처: harness-builder

## 책임

- {계약의 역할 행에서 — 이 에이전트가 맡는 일 (1~3줄)}

## 입력 (소비 아티팩트)

- {앞 역할이 넘긴 아티팩트. 없으면 "원 요청"}

## 출력 (생산 아티팩트)

- {이 역할이 남기는 아티팩트 — 부모가 다음 역할에 전달}

## 절차

1. {단계}
2. {단계}

## 하지 말 것

- {역할 경계 — 다른 역할 영역 침범 금지}
- 부수효과(파일 수정·외부 전송·예약·시스템 기록)가 필요하면 실행하지 말고 부모에게 반환한다 —
  **부수효과 게이트는 부모 하네스가 사용자 승인으로 통과시킨다** (승인 전 읽기 전용 탐색·초안만).

## 반환

- 최종 메시지로 **{생산 아티팩트}만** 반환한다 (부모가 종합한다 — 중간 과정 서술 최소화).
